# Check your understanding

1. What is the purpose of generateText() function when working with LLMs?
2. What is a system prompt in the context of LLMs?
3. What is the purpose of AI SDK's provider adapter modules like @ai-sdk/openai?
4. What are the three main parameters required to call the generatedText function()?
5. What is the core concept of how LLMs work at their most basic level?
6. What does the generateText() function do in the AI SDK?
    (a) It takes a prompt and generates text using a specified model
    (b) It compresses text data for storage
    (c) It converts audio input into the text format
    (d) It validates user input before processing
7. What is the purpose of system prompt in an LLM?
    (a) To validate the API key before making requests
    (b) To compress the conversation history for storage
    (c) To format the user's input into proper syntax
    (d) To provide base instructions and prime the LLM with background context.
8. What are the three arguments that the runAgent()function accepts?
    (a) A user message string, conversation history array, and callback object
    (b) A prompt string token limit, and response format
    (c) A system prompt, user ID, and session timeout
    (d) An API key, model name, and temperature setting
9. What is the purpose of the @ai-sdk/openai adapter module?
    (a) It caches responses to reduce API costs
    (b) It allows switching between different model providers using the same SDK
    (c) It converts Typescript code to JavaScript automatically
    (d) It encrypts API keys before sending requests
    